# ycy记账 — 产品文档与项目规则

## 一、产品概述

- **应用名称**：ycy记账
- **产品定位**：个人日常记账工具，帮助用户记录每一笔人民币花销
- **目标平台**：Windows 和 macOS（通过浏览器运行）
- **目标用户**：个人用户，无需多用户/登录系统

## 二、核心功能

### 2.1 记账记录
- 记录每一笔花销的**金额**（人民币/元）
- 记录花销的**日期和时间**
- 为每笔花销选择**支出分类**（两级：一级大类 + 二级小类）
- 可填写**备注**（可选，文字说明）

### 2.2 花销分类体系（两级）

| 一级大类 | 二级小类 |
|---------|---------|
| 🍽️ 餐饮饮食 | 早餐、午餐、晚餐、零食饮料、水果、外卖、买菜做饭 |
| 🚗 交通出行 | 公交地铁、出租车/网约车、加油充电、停车费、火车高铁、飞机 |
| 🛒 购物消费 | 衣服鞋帽、日用品、数码电子、家居装饰、美妆护肤 |
| 🏠 住房生活 | 房租、水电燃气、物业费、网费电话、维修保养、日杂 |
| 💊 医疗健康 | 看病买药、体检、健身运动、保健品 |
| 📚 教育学习 | 书籍、课程培训、文具、考试报名 |
| 🎮 娱乐休闲 | 电影演出、游戏、旅游度假、KTV/酒吧、宠物 |
| 🎁 人情往来 | 送礼红包、请客吃饭、婚礼份子、捐款 |
| 💰 投资理财 | 股票基金、保险、储蓄、借贷还款 |
| 📦 其他支出 | 快递物流、手续费、其他 |

### 2.3 数据查看
- 按时间查看花销列表（按日期排序）
- 按分类筛选花销
- 查看每日/每月支出汇总

### 2.4 数据持久化
- 所有数据保存在浏览器本地存储（localStorage）
- 数据不上传到任何服务器
- 关闭再打开数据还在（除非清除浏览器数据）

---

## 三、技术栈

> ✅ **已决定于 2026-07-01**

| 项目 | 选择 | 说明 |
|------|------|------|
| 运行方式 | **PWA + APK** | 浏览器 / 安卓安装包 |
| 安卓支持 | ✅ APK 安装包 | 可直接安装到安卓手机 |
| iOS 支持 | ✅ PWA（添加到主屏幕） | Safari 打开 → 添加到桌面 |
| UI 构建方式 | React + TypeScript | 最流行的前端技术 |
| 构建工具 | Vite | 秒级启动和打包 |
| 视觉风格 | Tailwind CSS | 清爽极简风（用户选择方案A） |
| 数据存储 | SQLite（sql.js WASM版） | 在浏览器中运行的轻量数据库 |
| UI 布局 | 标签页式 | 3个标签：记账、明细、统计 |

### 为什么从 Electron 改为网页版？

原计划使用 Electron 打包为桌面应用。但在开发过程中发现，当前最新的 Electron（42-43 版本）发生了重大架构变更，不再支持传统的 API 访问方式。经过多轮调试和方案尝试后，决定先用**纯网页版**交付：

- **优点**：无需安装、不占磁盘空间、任何浏览器都能用、开发迭代快
- **缺点**：不能像桌面应用那样有独立窗口（后续可解决）
- **后续计划**：等 Electron 生态稳定后，可以快速包装成桌面应用（代码无需改动）

---

## 四、如何运行

### 📱 安卓手机 — 安装 APK

1. 将 `ycy记账.apk` 文件传到手机上（微信/QQ/数据线均可）
2. 在手机上点击 APK 文件 → 允许安装未知来源应用
3. 安装完成后桌面出现 **ycy记账** 图标
4. 点开即可使用，完全离线可用

### 📱 安卓手机 — PWA（备用方式）

1. 用手机 Chrome 打开应用网址
2. 自动弹出 **"添加到主屏幕"** → 桌面出现图标
3. 之后像普通App一样使用

### 💻 电脑上开发/构建

```bash
cd ycy-accounting
npm install
npm run dev          # 开发模式，浏览器预览
npm run build        # 构建网页版
```

### 📦 重新打包 APK

```bash
# 前提：已安装 Java 21 + Android SDK
export ANDROID_HOME="C:/android-sdk"
export JAVA_HOME="C:/Program Files/Amazon Corretto/jdk21.0.11_10"

cd ycy-accounting
npm run build                          # 构建网页
npx cap sync android                   # 同步到安卓项目
cd android && ./gradlew assembleDebug  # 打包 APK
# APK 输出位置: android/app/build/outputs/apk/debug/app-debug.apk
```

---

## 五、项目规则（必须遵守）

### ⚠️ 最重要的规则：技术决策必须由用户拍板

**用户是编程小白，无法提供技术细节要求。因此：**

1. **任何涉及技术选择的事项**（选什么库、用什么架构、代码怎么写、UI布局怎么设计、颜色用什么等），必须：
   - 列出 2-4 个可选方案
   - 用通俗语言向用户解释每个方案的**意思、优缺点、适用场景**
   - 给出你的推荐，但最终由用户选择决定
   - 在用户决定之前，不得擅自推进

2. **不要假设用户知道**任何技术术语。解释时避免使用"框架"、"库"、"API"、"前端"、"后端"等词汇，或者使用时附带通俗解释。

3. **每次只问一个维度的决策**，不要一次性抛出大量问题让用户选择。

4. 这条规则适用于**整个项目开发周期**，直到用户主动说"以后技术细节你直接决定"为止。

### 其他规则

- 所有代码注释使用中文
- 数据100%本地存储，不联网
- 只做用户要求的功能，不额外加功能

---

## 六、项目状态

✅ **初版已完成！** — 核心功能 + PWA + APK 全部就绪：
1. ✅ 记账（金额输入 + 两级分类选择 + 日期 + 备注）
2. ✅ 明细（按日期分组列表 + 月份/分类筛选 + 删除）
3. ✅ 统计（月份选择 + 月度总额 + 分类支出排行 + 比例条）
4. ✅ PWA 支持（浏览器添加桌面 + 离线使用）
5. ✅ APK 安装包（`ycy记账.apk`，可直接安装到安卓手机）

### 待定事项（后续由用户决定）
- 数据导出/导入功能
- 预算管理
- 收入记录
- 更丰富的图表
